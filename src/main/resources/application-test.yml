spring:
  datasource:
    url: jdbc:h2:mem:user_test
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
jwt:
  secret: default-very-long-secret-key-at-least-256-bits-long-for-hmac-sha256
